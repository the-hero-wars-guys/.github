name: Reusable CodeQL

# CodeQL static analysis. Default language matrix is `actions` (analyzes
# GitHub Actions workflows for injection / supply-chain bugs). Consumers
# with additional languages (e.g. python tools, javascript) can extend the
# matrix via the `languages` input.
#
# Caller pattern:
#   uses: <owner>/<template-repo>/.github/workflows/reusable-codeql.yaml@<sha>
#   permissions:
#     contents: read
#     security-events: write
#     actions: read

on:
  workflow_call:
    inputs:
      languages:
        description: |
          JSON array of CodeQL languages to analyze. Defaults to ["actions"]
          which scans only the workflow YAML files. CodeQL has no native
          support for HashiCorp HCL (Terraform, Packer) or Ansible playbook
          YAML, so stack-specific source code is not analyzed by default.
          Consumers with additional language coverage (e.g. python tooling,
          javascript build scripts) can extend the matrix via this input:
          '["actions","python"]'.
        required: false
        type: string
        default: '["actions"]'
      timeout_minutes:
        description: Job timeout. Larger codebases may need more.
        required: false
        type: number
        default: 30

permissions: {}

jobs:
  analyze:
    name: CodeQL (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: ${{ inputs.timeout_minutes }}
    permissions:
      contents: read # required by CodeQL to analyze repository contents
      security-events: write # required to upload CodeQL SARIF to GitHub Security
      actions: read # required by CodeQL for workflow analysis metadata

    strategy:
      fail-fast: false
      matrix:
        language: ${{ fromJSON(inputs.languages) }}

    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Initialize CodeQL
        uses: github/codeql-action/init@e46ed2cbd01164d986452f91f178727624ae40d7 # v4.35.3
        with:
          languages: ${{ matrix.language }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@e46ed2cbd01164d986452f91f178727624ae40d7 # v4.35.3
        with:
          upload: never
          output: codeql-results

      - name: Upload CodeQL SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@e46ed2cbd01164d986452f91f178727624ae40d7 # v4.35.3
        # Best-effort publishing: GitHub Security SARIF upload can be
        # unavailable on private repos, but the analysis above remains the gate.
        continue-on-error: true
        with:
          sarif_file: codeql-results
